/*
Define an enum for the days of the week. Write a program to take a day’s integer value (e.g., 1 for Sunday, 2 for Monday) from the user and display the corresponding day’s name.
*/

#include <stdio.h>
enum Weekday { Sunday, Monday, Tuesday, Wednesday, Thursday, Friday, Saturday };
int main() {

enum Weekday today = Monday;

printf("Numeric value of today (Monday): %d\n", today);

today = Friday;

printf("Numeric value of today (Friday): %d\n", today); // Output: 5
return 0;

printf("\nName: Rajveer Chaudhari \n");
printf("Roll Number: 202411024\n");

}

